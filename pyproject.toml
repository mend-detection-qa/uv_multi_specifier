[project]
name = "test-uv-multi"
version = "0.1.0"
description = "UV test with multi-specifier and exclusion"
requires-python = ">=3.10,!=3.11.*"
dependencies = [
    "requests==2.31.0",
    "Django==2.2.0",
    "flask==2.2.5",
]
